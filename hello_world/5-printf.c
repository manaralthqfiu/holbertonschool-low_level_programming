#include <stdio.h>
/**
 * main -Entry point of the program
 *
 * Descripition: prints "with proper grammar, but the outcome is a piece of art"
 *
 * Return: Always 0 (Success)
 */
int main(void)
}
	printf("with proper grammar, but the outcome is a piece of atr,\n");
	return (0);
}
